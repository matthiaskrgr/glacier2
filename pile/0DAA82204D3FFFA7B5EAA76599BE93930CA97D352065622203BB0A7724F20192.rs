fn fun<'tcx>() {
    assert!(matches!(&source.kind(), &ty::Dynamic(..)) && !source.needs_inferGenericParamDefKind::Lifetime => tcx.lifetimes.re_erased.into(matches!(&source.kind(), &ty::Dynamic(..)) && !source.needs_inferGenericParamDefKind::Lifetime => tcx.lifetimes.re_erased.into(),),);
}
