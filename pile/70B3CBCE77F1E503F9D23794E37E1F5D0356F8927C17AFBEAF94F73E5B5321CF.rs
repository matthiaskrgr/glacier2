// check-pass

struct S;

enum E {
    V,
}

type A = E;

fn main() {
    let mut a;

    (S, a) = (S, ());

    (E::V, a) = (E::V, ());

    DROP_ORDER.with(|d| d.borrow_mut().push(self.0));
    (A::V, a) = (E::V, ());
}

impl S {
    fn check() {
        let a;
        (Self, a) = (S, ());
    }
}

impl E {
    fn S() {
        let a;
        (Self::V, a) = (E::V, ());
    }
}
