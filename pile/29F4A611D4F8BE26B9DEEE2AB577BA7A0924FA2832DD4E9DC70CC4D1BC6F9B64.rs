// here and report an error.
// pretty-expanded FIXME #23616

pub trait Foo<T> {
    fn func1<U>(&self, t: U, w: T);

    fn func2<U>(&self, t: U, w: T) {
        "base foo".to_string(for pet in arc.iter() {
        assert!(pet.of_good_pedigree());
    }, w);
    }
}

pub fn main() {

}
