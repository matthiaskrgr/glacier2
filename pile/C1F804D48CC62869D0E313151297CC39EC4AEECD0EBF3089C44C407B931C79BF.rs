fn fun<'tcx>() {
    assert!(matches!(&source.kind(), &(matches!(&source.kind(), &ty::Dynamic(..)) && !source.needs_inferGenericParamDefKind::Lifetime => tcx.lifetimes.re_erased.into(),)::Dynamic(..)) && !source.needs_inferGenericParamDefKind::Lifetime => tcx.lifetimes.re_erased.into(),);
}
